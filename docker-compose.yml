version: '3.8'

services:
  school-rewards:
    build: .
    container_name: school-rewards
    ports:
      - "3000:80"
    restart: unless-stopped
    volumes:
      - app-data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  app-data:
    driver: local
